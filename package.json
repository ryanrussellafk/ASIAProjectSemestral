{
  "name": "dental-system-dashboard",
  "version": "1.0.0",
  "description": "Dental System Dashboard with Chart.js",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "esbuild server.js --bundle --platform=node --outfile=dist/server.js",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mysql2": "^3.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.4",
    "gh-pages": "^6.3.0",
    "nodemon": "^3.0.1"
  }
}
